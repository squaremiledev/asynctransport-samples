subprojects {

    ext {
        systemUnderTestVersion = property('dev.squaremile.asynctransport.version')
    }
    
    group 'dev.squaremile'
    version 'SNAPSHOT'

    repositories {
        jcenter()
        mavenLocal()
    }


}